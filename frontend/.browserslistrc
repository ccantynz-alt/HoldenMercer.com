# Browser support targets for build tooling (Vite/PostCSS/etc).
# Modern evergreen browsers + iOS Safari 14+ for the iPad PWA path.
defaults
not dead
not op_mini all
not IE 11
last 2 versions
iOS >= 14
Safari >= 14
